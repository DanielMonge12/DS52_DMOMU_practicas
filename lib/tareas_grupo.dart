import 'package:flutter/material.dart'; 

class tareas_grupo extends StatelessWidget { 
  @override
  Widget build(BuildContext context) { 
    return Scaffold( 
      appBar: AppBar( 
        title: Text('Tareas'), 
      ),
      body: Center( 
        child: Text('tareas de grupo'), 
      ),
    );
  }
}
